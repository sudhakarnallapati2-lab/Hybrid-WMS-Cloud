version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-wms-api
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    restart: unless-stopped
